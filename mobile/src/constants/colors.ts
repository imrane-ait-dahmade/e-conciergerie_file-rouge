/**
 * Réexport du thème couleur.
 * Préférer : `import { palette, colors } from '@/src/theme/colors'`.
 */
export { Brand, Colors, colors, palette } from '@/src/theme/colors';
export type { ColorName, PaletteName } from '@/src/theme/colors';
